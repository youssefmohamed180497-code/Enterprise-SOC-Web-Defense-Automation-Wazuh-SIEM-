if you want install it Native in ubunto directly not container 
#curl -sO https://packages.wazuh.com/4.14/wazuh-install.sh && sudo bash ./wazuh-install.sh -a                				install Wazuh after install should output INFO: Installation finished.
Access	 https://<WAZUH_DASHBOARD_IP_ADDRESS>

🟢 1#install in container
------------------------
cd ~
git clone https://github.com/wazuh/wazuh-docker.git -b v4.14.1
cd wazuh-docker/single-node
sudo docker-compose -f generate-indexer-certs.yml run --rm generator								certificate generator
sudo docker-compose up -d
sudo docker ps													we should see 3 running (manager , indexer , dashboard )
https://<SERVER-IP>
Username: admin
Password: SecretPassword


-----   other way  install_wazuh_docker.sh    -------		
nano install_wazuh_docker.sh											copy that and paste 

apt update -y
apt install -y docker.io docker-compose-plugin git
sysctl -w vm.max_map_count=262144
echo "vm.max_map_count=262144" >> /etc/sysctl.conf
cd /root
git clone https://github.com/wazuh/wazuh-docker.git -b v4.14.1
cd wazuh-docker
docker compose -f generate-indexer-certs.yml run --rm generator
cd single-node
docker compose up -d

chmod +x install_wazuh_docker.sh
./install_wazuh_docker.sh												Run it 
https://<SERVER-IP>
